
public double multiply(ArrayList<Double> list)
    {
        // number X 1 = number
        double product=1;
        //iterate through the operands
        for (double i:list)
        product*=(double)i;
        //simply multiply every number and return the answer
        return product;
    }
	